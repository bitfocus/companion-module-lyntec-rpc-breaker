{
	"name": "lyntec-rpc-breaker",
	"version": "2.0.0",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "prettier -w .",
		"lint:raw": "eslint --ext .ts --ext .js --ignore-pattern dist --ignore-pattern pkg",
		"lint": "yarn lint:raw ."
	},
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/bitfocus/companion-module-lyntec-rpc-breaker.git"
	},
	"dependencies": {
		"@companion-module/base": "~1.4.1"
	},
	"devDependencies": {
		"@companion-module/tools": "^1.3.1"
	}
}
